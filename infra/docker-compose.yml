version: "3.8"

services:
  postgres: 
    image: postgres:${POSTGRES_VERSION}
    container_name: postgres
    hostname: ${POSTGRES_HOST}
    restart: on-failure
    ports:
      - ${POSTGRES_PORT}:${POSTGRES_PORT}
    volumes:
      - lenta_postgresql_data:/var/lib/postgresql/data/
    env_file:
      - ./.env

  backend:
    build: ../
    container_name: lenta-backend
    restart: on-failure
    volumes:
      - lenta_static_data:/app/static/
      - lenta_media_data:/app/media/
    depends_on:
      - postgres
    env_file:
      - ./.env
    command: sh -c 'python manage.py collectstatic --noinput && python manage.py makemigrations users sales && python manage.py migrate && python manage.py createsuperuser --noinput && python manage.py load_sku_data && python manage.py load_shop_data && python manage.py load_sales_data && python manage.py dumpdata > fixtures.json'

      
  nginx:
    image: nginx:${NGINX_VERSION}
    container_name: nginx
    restart: on-failure
    ports:
      - 80:80

    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - lenta_static_data:/var/html/static/
      - lenta_media_data:/var/html/media/

    depends_on:
      - backend

    env_file:
      - ./.env

volumes:
  lenta_postgresql_data:
  lenta_static_data:
  lenta_media_data:
